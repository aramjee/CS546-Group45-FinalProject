{{!

This page will respond with a valid HTML document. The title of the document should be "Venue Finder". You should have
the title set as the <title> element of the HTML document and as an h1 in your document.

    Your page should reference a CSS file, /public/css/main-styles.css; this file should have at least 5 rulesets that
    apply
    to this page; these 5 rules can also apply to elements across all of your pages, or be unique to this page.

    You should have a main element, and inside of the main element have a p element with a brief (2-3 sentence
    description) of what your website does.

    Also inside the main element, you will have a form; this form will POST to the /searchvenues route. This form will
    have an input and a label; the label should properly reference the same id as the input using the for attribute. You
    should also have an input with a type of submit that submits the form. The input in your form should have a name of
    searchVenueTerm.

    }}
    <div class="row flex-lg-row-reverse align-items-center g-5 py-5">
        <div class="col-10 col-sm-8 col-lg-6">
            <img src="public\images\Squatter.png" class="d-block mx-lg-auto img-fluid" alt="Bootstrap Themes"
                width="700" height="500" loading="lazy">
        </div>
        <div class="col-lg-6">
            <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">Welcome</h1>
            <p class="lead">This gym review platform focuses just on the Hoboken region, where gym users can view
                and provide feedback (reviews) on gyms. Users will be able to click on a gym page which will navigate
                them to a page where standard information resides such as the address, operating hours, website, phone
                number and additionally rate/see gym attributes such as equipment (quality and/or type), trainer
                availability, atmosphere, locker room, etc
            </p>

        </div>
    </div>


    <main>

        <p>This website connects to the master API, and gets the data that you search for. In this website, you are
            using HTML templating to generate the html.</p>
        <form action="/searchvenues" method="POST">
            <label for="searchVenueTerm">Search Term</label>
            <input type="text" name='searchVenueTerm' id="searchVenueTerm">
            <input type="submit" value="submit">
        </form>
    </main>
